@* @using Cinema_Project.ViewModel
@model CreateActorVM
<h1 class="text-center mt-5">Create Actor</h1>

<form method="post" class="mt-5 container-fluid" enctype="multipart/form-data">
    <div asp-validation-summary="All" class="text-danger mb-3"></div>
    <!-- Name -->
    <div class="input-group mb-3">
        <span class="input-group-text">
            <i class="fa fa-tag"></i>
        </span>
        <input class="form-control" placeholder="Name" asp-for="Name">
        <span class="text-danger" asp-validation-for="Name"></span>
    </div>

    <!-- Main Img -->
    <div class="mb-3">
        <label class="form-label">Actor Img</label>
        <input type="file"
               class="form-control"
               asp-for="actorImg"
               accept="image/*"
               onchange="previewImage(event)">
        <span class="text-danger" asp-validation-for="actorImg"></span>
    </div>

    <!-- Preview -->
    <div class="mb-3">
        <img id="preview"
             style="max-width: 200px; border:1px solid #ccc; padding:5px; border-radius:10px; display:none;" />
    </div>

    <!-- Button -->
    <button type="submit" class="btn btn-success">Submit</button>

</form>
<script>
    function previewImage(event) {
        const input = event.target;
        const preview = document.getElementById("preview");

        if (input.files && input.files[0]) {
            const reader = new FileReader();

            reader.onload = function (e) {
                preview.src = e.target.result;
                preview.style.display = "block"; // يظهر الصورة
            };

            reader.readAsDataURL(input.files[0]);
        }
    }
</script>
@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <partial name="_NotificationPartial" />
}
 *@

@using Cinema_Project.ViewModel
@model CreateActorVM

<h1 class="text-center mt-5">Create Actor</h1>

<form method="post" class="mt-5 container-fluid" enctype="multipart/form-data">

    @Html.AntiForgeryToken()

    <div asp-validation-summary="All" class="text-danger mb-3"></div>

    <!-- Name -->
    <div class="input-group mb-3">
        <span class="input-group-text">
            <i class="fa fa-tag"></i>
        </span>
        <input class="form-control" placeholder="Name" asp-for="Name">
        <span class="text-danger" asp-validation-for="Name"></span>
    </div>

    <!-- Main Img -->
    <div class="mb-3">
        <label class="form-label">Actor Img</label>
        <input type="file"
               class="form-control"
               asp-for="actorImg"
               accept="image/*"
               onchange="previewImage(event)">
        <span class="text-danger" asp-validation-for="actorImg"></span>
    </div>

    <!-- Preview -->
    <div class="mb-3">
        <img id="preview"
             style="max-width: 200px; border:1px solid #ccc; padding:5px; border-radius:10px; display:none;" />
    </div>

    <button type="submit" class="btn btn-success">Submit</button>

</form>

<script>
    function previewImage(event) {
        const input = event.target;
        const preview = document.getElementById("preview");

        if (input.files && input.files[0]) {
            const reader = new FileReader();

            reader.onload = function (e) {
                preview.src = e.target.result;
                preview.style.display = "block";
            };

            reader.readAsDataURL(input.files[0]);
        }
    }
</script>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <partial name="_NotificationPartial" />

}